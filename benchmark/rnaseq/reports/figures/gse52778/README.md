# GSE52778 figures

Figures 1–3 were rendered on a Slurm compute node with the certified R 4.3.3
analysis environment. Figure 4 is the native HelixForge PCA copied without
modification. PNG files are used by the report; vector PDF versions are kept
for figures 1–3. `render_sessionInfo.txt` records the rendering runtime.

