# Stage 9B.1 protocol/implementation audit

Audit date: 2026-08-25

| ID | Classification | Observation | Resolution before execution |
|---|---|---|---|
| 9B1-D01 | `BENCHMARK_FINDING` | The Stage 9B.1 request still names `benchmarks/rnaseq/`, but the repository's pre-existing canonical directory is singular `benchmark/`. | All benchmark-only implementation and evidence use `benchmark/rnaseq/`. No second top-level directory is created. |
| 9B1-D02 | `BENCHMARK_FINDING` | Stage 9A described downloading the complete GENCODE GTF/genome bundle, while Stage 9B.1 requires a tightly controlled synthetic reference and warns against using GENCODE if it weakens ground truth. | Use only the registered GENCODE v49 all-transcript FASTA as sequence source. Deterministically select 1,200 genes/2,400 real isoforms, then generate a closed pseudo-genome, GTF and tx2gene containing only those records. This preserves real within-gene sequence ambiguity and exact gene↔transcript truth. |
| 9B1-D03 | `BENCHMARK_FINDING` | The 9A JSON defined condition count but did not enumerate sample IDs and condition membership. | `synthetic_design.json` now freezes all six samples and replicates explicitly. |
| 9B1-D04 | `BENCHMARK_FINDING` | The 9A JSON froze the number and size of DE effects but left the assignment order implicit. | The contract now hashes gene IDs with the fixed selection seed and assigns fixed consecutive up/down effect blocks. |
| 9B1-D05 | `BENCHMARK_FINDING` | The 9A truth contract did not state how transcript/gene TPM is derived from the exact Polyester fragment counts. | The JSON now freezes length-normalized transcript TPM and gene aggregation formulas. |
| 9B1-D06 | `BENCHMARK_FINDING` | The RC `full` workflow only runs the candidate-gene Report API when `rnaseq_report_enabled=true`, while the frozen 9A synthetic run matrix deliberately disables it to prevent truth-derived gene selection. | The synthetic benchmark validates the standard QC/MultiQC and terminal reporting/provenance outputs but classifies the optional candidate-gene report as `NOT_APPLICABLE`. The public-data stage will exercise it with a predeclared list. |
| 9B1-D07 | `ENVIRONMENTAL_FAILURE` | The reusable cluster `rna-tools` environment contains Java 23.0.2 and MultiQC 1.30; `r-analysis` contains DESeq2 1.42.1. These differ from the RC locks Java 21, MultiQC 1.17 and DESeq2 1.42.0. | Reuse the already certified Temurin 21 runtime, but create benchmark-specific exact Conda prefixes for the scientific tools. Existing shared environments remain untouched. |
| 9B1-D08 | `BENCHMARK_FINDING` | The initial runtime preflight invoked FastQC by absolute path without putting Java on `PATH`; FastQC therefore printed a missing-Java message although the preflight job completed. | Export the combined explicit runtime PATH before version probes and repeat the preflight. Job `15165` is retained as superseded provenance, not scientific evidence. |
| 9B1-D09 | `ENVIRONMENTAL_FAILURE` | The mutable validation path `nextflow.jar` contained Nextflow 26.04.6 when the primary run was attempted. | The fail-closed driver stopped before submission. Use the existing immutable framework artifact `nextflow-25.10.7-one.jar`; record the exact path/version and retain the blocked attempt in the audit trail. |
| 9B1-D10 | `ENVIRONMENTAL_FAILURE` | The portable Temurin 21 runtime used by the Nextflow driver could not discover the Conda fontconfig stack when FastQC rendered its report (`Fontconfig head is null`). | Keep Nextflow on Java 21. Run FastQC with the JVM and fonts resolved inside the exact `rna-tools-rc` environment, expose its fontconfig paths to Slurm tasks, and record this tool JVM separately. Probe `15202` produced complete HTML/ZIP; the failed workflow attempt remains preserved. |
| 9B1-D11 | `ENVIRONMENTAL_FAILURE` | The shared `python-list` prefix lacks `jsonschema`, so the completed scientific path could not emit the terminal `RUN_MANIFEST`. | Preserve the failed attempt; create a clean benchmark prefix with Python 3.12.4 and jsonschema 4.23.0 exactly as declared by the module. Do not modify the shared prefix or the RC. Repeat the primary run from a clean case root after a fail-closed preflight. |
| 9B1-D12 | `ENVIRONMENTAL_FAILURE` | `pipeline_config.sh` activates `PYTHON_ENV` as a Conda environment name, so replacing `python-list` with the absolute benchmark-prefix basename caused the compatibility adapter to stop before QC. | Preserve the failed clean case. Keep `PYTHON_ENV=python-list` for the RC compatibility adapter, while the benchmark driver prepends the exact Python 3.12.4/jsonschema 4.23.0 prefix to the Slurm task `PATH`. This leaves the RC configuration contract intact and gives `RUN_MANIFEST` the certified runtime. |
| 9B1-D13 | `BENCHMARK_FINDING` | The first structural validator expected a process literally named `TXIMPORT`, while the RC trace records the provider implementation as `SALMON_IMPORT`; the completed outputs remain the tximport contract (`counts_matrix.tsv`, `tpm_matrix.tsv`, `length_matrix.tsv`, and `summarized_experiment.rds`). | Validate the implemented provider name `SALMON_IMPORT` and the complete Import API artifacts. Preserve validator job `15419` as superseded benchmark evidence. |
| 9B1-D14 | `BENCHMARK_FINDING` | The second structural validator compared Salmon's processed pairs with the 2,000,000 raw simulated pairs, ignoring the valid paired reads removed by Trim Galore. | Count records independently in both post-trim merged FASTQs; require equal mates and exact agreement with Salmon `num_processed`. Retain raw-pair count only to report the retention rate. Preserve validator job `15420` as superseded benchmark evidence. |
| 9B1-D15 | `BENCHMARK_FINDING` | Salmon's RC indexing policy has `keep_duplicates=false` and removed 24 sequence-identical transcripts, leaving 2,376 indexed transcripts and 1,195 estimable genes from the frozen 2,400-transcript/1,200-gene input. The third validator incorrectly required 1,200 rows after Import/DE. | Require the 1,200/2,400 input contract, verify the exact duplicate-removal policy/log, derive the estimable gene universe from indexed transcripts, and require Import and DE to preserve that complete 1,195-gene universe. Preserve validator job `15421` as superseded evidence and report both input and estimable universes. |
| 9B1-D16 | `BENCHMARK_FINDING` | The RC publishes the terminal RNA-seq manifest both under `pipeline_info/integration_api/` and `rnaseq/`; validator job `15422` incorrectly required a single physical copy. | Require at least one terminal manifest, byte-identical content across all published copies, and terminal status `complete`. Both relative paths are retained in the validation report. |
| 9B1-D17 | `BENCHMARK_FINDING` | Independent-harness job `15424` completed Salmon, tximport and DESeq2 but failed while writing provenance because base R's `numeric_version` object from `getRversion()` was passed directly to `cat()`. | Preserve the completed-but-unterminated attempt, serialize the R version with `as.character(getRversion())`, and repeat the independent harness in a new fail-closed output root. No scientific command or parameter changes. |
| 9B1-D18 | `BENCHMARK_FINDING` | Comparison job `15426` required identical DE row order. HelixForge orders its aggregate DE table by significance, whereas the independent DESeq2 harness retains input gene order; the DE contract requires identical gene identity and values, not presentation order. | Keep strict ID/order checks for `quant.sf` and Import matrices, but compare DE by an identical unique gene-ID set. Add DEG-set, direction, p-value rank and top-N overlap metrics. Preserve the failed comparator job as superseded evidence. |
| 9B1-D19 | `BENCHMARK_FINDING` | The first completed independent harness used all four allocated CPUs for Salmon, while the RC trace artifacts record `threads=1`. The same inputs and tool version produced identical DEG sets but small quantification/DE numeric differences beyond the frozen tolerance. | Preserve independent job `15425` and comparison job `15431`. Repeat the independent harness with one allocated CPU so its Salmon command matches the RC execution exactly; do not loosen tolerance or modify HelixForge. |
| 9B1-D20 | `BENCHMARK_FINDING` | One-thread independent job `15432` still diverged numerically, while retaining all 129 DEGs and top-N sets. Identical Salmon 1.10.3 index commands produced the same sequence/name hashes but different `ctable.bin`, `pos.bin`, and `seq.bin` artifacts, demonstrating index-build sensitivity rather than an orchestration/contract mismatch. | Preserve rebuilt-index comparison job `15433` as a sensitivity result. Add a separately labeled strict arm that invokes Salmon/tximport/DESeq2 independently but reads the immutable RC-published index; this isolates quantification/import/DE semantics without loosening the frozen tolerance. Report both arms. |
| 9B1-D21 | `BENCHMARK_FINDING` | Two one-thread independent harness runs (`15434`, `15437`) using the same immutable RC index still diverged under strict numeric tolerance; comparison job `15438` retained identical DEG and top-N sets. Salmon 1.10.3 `quant --help-reads` captured by job `15436` exposes no global random-seed control. | Classify byte/numeric repeatability as a Salmon runtime limitation for this RC. Keep the strict gate failed and report set/rank/direction stability separately; do not modify the frozen RC or silently widen tolerance. |
| 9B1-D22 | `ENVIRONMENTAL_FAILURE` | The first clean top-level repeat used the directory containing benchmark prefixes as `CONDA_BASE`; the RC adapter requires the Miniconda installation containing `bin/conda` and stopped at `RNASEQ_QUANTIFICATION_PLAN`. | Preserve the failed clean case. Rebuild inputs in a new root using the same `/home/.../miniconda3` base as the validated primary case; exact task runtimes remain selected by their registered environment names and benchmark driver `PATH`. |
| 9B1-D23 | `SANITY_CHECK` | MultiQC completed but received only FastQC ZIPs. It found 24 reports after duplicate sample-name overwrites and contains neither Cutadapt/Trim Galore nor Salmon; Salmon cannot appear because the current QC MultiQC barrier precedes quantification. | Keep the completed report as valid FastQC aggregation, record missing sections as a non-gating sanity finding, and recommend a future terminal MultiQC/report aggregation step with collision-safe sample labels. Do not alter the RC during its benchmark. |

## Confirmed RC implementation

The audited RC path is:

```text
RNASEQ_CONTEXT -> RNASEQ_METADATA -> REFERENCE_BUNDLE
  -> FASTQC(raw) -> TRIM_GALORE -> FASTQC(trimmed)
  -> MERGE_FASTQ -> FASTQC(merged) -> MULTIQC
  -> SALMON_INDEX -> SALMON_QUANT
  -> TX2GENE_BUILD -> TXIMPORT
  -> DE_PREFLIGHT -> DESEQ2_MODEL -> DESEQ2_CONTRAST -> DE_AGGREGATE
  -> RUN_MANIFEST
```

The benchmark uses `rnaseq_run_mode=full`,
`rnaseq_analysis_mode=quantification`, `rnaseq_native_alignment=false`,
`production_v1`, `full_length`, `lengthScaledTPM`, and the frozen
`~ condition` contrast. STAR and automatic batch correction are absent.

No discrepancy above modifies HelixForge scientific code, module parameters,
schemas or statistical thresholds.
