# Scientific deviation log

This log records discovered differences, their scientific relevance, and the
decision required before retiring a legacy component. No entry is waived merely
because a native process completes.

| ID | Component | Observation | Scientific impact | State | Required action |
|---|---|---|---|---|---|
| SD-001 | Trim Galore fixture | FASTQ qualities were two bases longer than their sequences. | Test could not exercise trimming. No production algorithm impact. | RESOLVED | Fixture corrected; real regression passes. |
| SD-002 | STAR | Unescaped `$` in a Bash regex shifted Nextflow template substitutions. | Index, reads, threads and output arguments were rendered incorrectly. | RESOLVED | End anchor escaped; legacy/native semantic regression passes. |
| SD-003 | BAM blacklist | Valid CRLF BED was rejected as non-numeric. | Cross-platform blacklist inputs could fail before filtering. | RESOLVED | Strip terminal CR before strict BED/contig validation. |
| SD-004 | Cache | The one-task probe resumes with Nextflow 25.10.7 on Java 21/23 and fails with 26.04.x on Java 21/23/25. However, a complete 25.10.7 RNA baseline followed by an identical run kept the same session UUID but produced no LevelDB task records and resubmitted jobs even with an explicit persistent `NXF_CACHE_DIR`. | Scientific execution is intact, but top-level compute reuse and every selective-invalidation claim remain uncertified. | BLOCKED-RUNTIME | Keep 25.10.7 pinned for runtime stability, preserve the full-DAG reproducer, and investigate the scale/configuration/shared-filesystem cache interaction with administrators or upstream. |
| SD-005 | Import checksums | `.sf` and `.tab` were converted to CRLF on Windows while manifests stored LF hashes. | Import correctly failed closed, but fixtures were not portable. | RESOLVED | Added LF attributes and normalized only affected fixtures. |
| SD-006 | Import regression policy | Regression configured prefix preservation while legacy strips transcript/gene prefixes and versions. | Gene/transcript identifiers differed despite equal numeric values. | RESOLVED-TEST | Regression now selects the API's `legacy` normalization policy. |
| SD-007 | RNA workflow Import policy | `production_v1` uses `lengthScaledTPM` for full-length and `no` for three-prime libraries, preserves IDs, and disables tximport truncation; the old behavior is explicit as `legacy_compatibility_v1`. | Production and historical outputs intentionally differ when legacy ID rewriting or original full-length counts mattered. | RESOLVED | Keep both production and historical regression scenarios; never select compatibility mode implicitly. |
| SD-008 | DESeq2 container | Previous image mutated an existing Biocontainers `/usr/local`; image 1.0.1 now builds one clean Micromamba environment and includes `procps`. | The certified runtime no longer depends on reconciling an inherited Conda prefix. | RESOLVED | CI build, scientific regression, and cache suite passed in run 31526144851. |
| SD-009 | DESeq2 obsolete image | ggplot2 3.4.4/ggrepel failed with missing `replace_null` after the model fit. | The obsolete image cannot complete analysis. | RESOLVED-BY-REPLACEMENT | Image 1.0.1 pins the declared compatible package set; do not reuse the obsolete image. |
| SD-010 | DE fixture | Initial synthetic counts caused the known all-dispersions-near-minimum failure. | Test measured a degenerate dataset, not provider behavior. | RESOLVED | Added replicate variability and updated manifest checksum. |
| SD-011 | Bowtie2 container | Configured Bowtie2 image lacks samtools used by `BOWTIE2_ALIGN`; a same-runtime Slurm comparison passed with the cluster environment. | The API behavior is supported, but the pinned Docker profile cannot produce BAM. | BLOCKED-PRODUCTION | Publish declarative Bowtie2 2.5.4 + samtools 1.20 image and record digest. |
| SD-012 | Metadata adapters | Python slim image lacks `ps`, required by Nextflow task metrics. | Docker profile fails before adapter logic. | BLOCKED | Build a small Python adapter image with procps, or use a documented supported base image. |
| SD-013 | MultiQC | Local download was unavailable, while Slurm used MultiQC 1.30. The dedicated Docker certification later executed MultiQC 1.17 on two deterministic FastQC records. | The reusable module, report/data outputs and declared production runtime are validated independently of host packages. | RESOLVED | OCI digest `sha256:fb7d6625fb5adaed43ced8bd051a875038714180bcfcd7c8e467204f72882de9` passed GitHub Actions run `31726522504`; retain this immutable reference. |
| SD-014 | MACS3 | Real two-replicate calling passes locally and on Slurm, but the adapter image and cache remain unresolved. | Peak calling itself is supported; complete Docker/cache certification is not. | OPEN-CONDITIONAL | Resolve SD-004 and SD-012, then rerun unchanged fixture with pinned images. |
| SD-015 | ChIP downstream | FRiP, union consensus, Differential Binding, annotation, tracks and report passed sequential controlled Slurm runs. | Reduced scientific contracts are supported, but this is not a full legacy or biological regression. | RESOLVED-CONDITIONAL | Keep legacy until pinned environments and one reviewed top-level biological regression pass. |
| SD-016 | Trim Galore provenance | Multiline version output was parsed as `failed.` instead of 0.6.10. | Scientific outputs were unaffected, but recorded provenance was false. | RESOLVED | Parse the explicit `version` line; real Slurm regression records 0.6.10. |
| SD-017 | STAR cluster runtime | Conda STAR 2.7.11b aborts with heap corruption after writing a tiny index using both AVX2 and plain binaries. | Target-cluster alignment cannot be certified with this runtime. | BLOCKED-ENVIRONMENT | Test the pinned production image or an administrator-supported STAR build; do not install an ad hoc replacement. |
| SD-018 | DESeq2 runtime | Slurm regression used DESeq2 1.42.1, while the separate OCI 1.0.1 regression uses the declared environment. | Scientific architecture and production image are both validated, though not in the same Slurm execution. | RESOLVED-CONDITIONAL | Repeat on Slurm with OCI/Apptainer when that runtime becomes available. |
| SD-019 | Slurm allocation | The cluster allocated two CPUs for jobs requesting one. | No scientific impact; tiny benchmark efficiency is distorted. | DOCUMENTED | Keep conservative queue limits and report allocated resources from `sacct`. |
| SD-020 | Bowtie2 cluster wrapper | The Conda prefix contains `@`, which breaks the Perl Bowtie2 dispatch wrapper while loading its generated configuration. | Cluster wrapper cannot launch, although the compiled binary is functional. | BLOCKED-ENVIRONMENT | Validation used an isolated direct-binary shim for both sides; administrators should rebuild the environment in a prefix safe for Perl. |
| SD-021 | Alignment test routing | STAR-only `--outTmpDir` was forwarded to Bowtie2 by the isolated test workflow. | Bowtie2 received an invalid option before alignment. | RESOLVED | Added provider-specific `bowtie2_extra_args`; production provider parameters remain unchanged. |
| SD-022 | Consensus staging | Replicate peak manifests were all named `manifest.json` and collided when staged into one context task. | Consensus could not start with real Peak Calling outputs. | RESOLVED | Stage each manifest collection member in a unique directory while preserving content. |
| SD-023 | Differential Binding aggregate | `db_aggregate.py` declared `--model` but accessed `args.models`. | Successful models and contrasts could not be consolidated. | RESOLVED | Access the declared `args.model` collection; real aggregate regression passes. |
| SD-024 | Packaged resource modes | Annotation, tracks and report Python resources had Git mode `100644`; a validator piped through `tee` initially masked permission failure. | Real native providers could not launch from module binaries. | RESOLVED | Mark direct resource executables `100755` and enable `pipefail` on affected logging pipelines. |
| SD-025 | IDR/report status | IDR remains `not_implemented`; the generated report therefore has status `incomplete`. | A complete report must not imply unavailable reproducibility evidence. | OPEN-DECISION | Preserve fail-honest status; implement IDR or explicitly exclude it from the first release scope. |
| SD-026 | Salmon planning | Unescaped Bash regex end anchors shifted Nextflow v2 template substitutions in `RNASEQ_QUANTIFICATION_PLAN`. | Salmon parameters and plan inputs were rendered in the wrong positions before quantification. | RESOLVED | Escape both end anchors; real top-level Salmon planning and four quantifications pass. |
| SD-027 | DE annotation | Sparse GFF3 files without optional `Name` or `biotype` produced zero-length annotation vectors. | DESeq2 model fit, but contrast export aborted before results were emitted. | RESOLVED | Fall back to gene ID and `Unknown`; retain `gene_biotype` when present. |
| SD-028 | MultiQC publication | Writing an external compatibility path from inside Docker failed because a `val` path is not mounted; the earlier manual replacement was also non-idempotent. | Scientific report generation passed, but portable publication initially failed. | RESOLVED | Publish the report and data directory through Nextflow `publishDir`; real Docker certification passed in run `31726522504`. |
| SD-029 | ChIP validation runtime | One combined `PATH` selected either an R environment without `jsonlite` or a Python environment without `pyBigWig`. | Differential Binding or track statistics could fail despite both dependencies already existing on the cluster. | RESOLVED-VALIDATION | Use an isolated compatibility directory for Bowtie2/Python while retaining `Rscript` from `r-analysis`; no environment was modified. |
| SD-030 | ChIP top-level fixture | Four consensus regions produced nearly uniform gene-wise dispersions, which DESeq2 correctly rejected. | The synthetic dataset could not exercise the unmodified default dispersion fit. | RESOLVED-TEST | Expanded the deterministic reduced fixture to 14 variable signal regions; the standard DESeq2 model and contrast pass unchanged. |
| SD-031 | ChIP report inventory | The harness expected the Differential Binding manifest one directory too high and supplied individual manifests of type `consensus`, unsupported by the Report API. | Report orchestration stopped after all upstream scientific stages had passed. | RESOLVED-TEST | Consume the published DB result manifest and aggregate `consensus_idr` manifest; Report context, aggregation and HTML generation pass. |
| SD-032 | Top-level ChIP-seq | The complete supported Slurm chain passed with 5 records, 100 scientific tasks, real QC/alignment/peaks/downstream artifacts and semantic validation. | The selected production path is operationally and scientifically supported on a reduced deterministic dataset. | RESOLVED-CONDITIONAL | Retain the legacy pipeline until pinned OCI/Apptainer runtimes and a reviewed biological regression are certified; IDR remains outside this pass. |

## Acceptance rule

An open scientific difference requires one of: correction, explicit documented
release policy, or a justified tolerance with evidence. Environment blockers may
be conditional, but they cannot be promoted to scientific equivalence.
