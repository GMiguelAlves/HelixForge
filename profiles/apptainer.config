process.executor = 'local'
apptainer.enabled = true

// Modules select their declared Apptainer source when one differs from OCI.
// This profile exists but remains operationally experimental until a supported
// site runtime can execute the complete smoke matrix.
