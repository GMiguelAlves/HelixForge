process.executor = 'local'
docker.enabled = true

// Preparation profile: compatibility images will be assigned per process in a
// later migration phase. No image is selected implicitly in this skeleton.

