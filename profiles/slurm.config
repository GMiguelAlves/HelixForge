process.executor = 'slurm'

executor {
    queueSize       = 5
    submitRateLimit = '10 sec'
}

// Supply cluster-specific partition, account, QoS, and scratch settings in a
// separate file passed with -c. Processes never submit nested jobs.
