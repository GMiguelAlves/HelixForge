process.executor = 'slurm'

executor {
    queueSize       = 100
    submitRateLimit = '10 sec'
}

// Supply cluster-specific partition, account, QoS, and scratch settings in a
// separate file passed with -c. Scientific scripts are always forced to local
// mode inside each Nextflow-managed Slurm allocation.

