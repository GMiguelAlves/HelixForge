process.executor = 'local'
conda.enabled = true

// Every scientific process resolves its module-owned environment.yml.
// Conda is supported architecturally but has not completed the RC smoke matrix.
