# Reference Bundle v1

A reference bundle gives every workflow a common `reference_id` and stable
locations for a genome and annotation. Only genome and annotation are required.
Transcripts, chromosome sizes, blacklist, and indexes are optional.

Indexes are derived artifacts and remain tool/version dependent. This skeleton
defines their locations but does not yet build or validate a bundle centrally;
the legacy reference scripts remain authoritative.

