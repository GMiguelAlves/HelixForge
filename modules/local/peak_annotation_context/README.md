# PEAK_ANNOTATION_CONTEXT

Preflight for Peak Annotation API v1. It joins no files by order or basename,
does not rewrite contigs/builds/IDs, and fails before provider execution on
identity, checksum, coordinate, seqname, parameter, or reference conflicts.

See `docs/peak_annotation_api.md` for the formal contract.
