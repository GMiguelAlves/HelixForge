# DB_AGGREGATE

Collects provider outputs without recomputing counts or statistics. The final
manifest links raw/normalized matrices, model objects and every independent
contrast for future annotation and Integration APIs.
