# RNASEQ_DE_CONTEXT

Reads `config/pipeline_config.sh` without replacing it and creates the explicit
Differential Expression API request for the legacy `all_projects/raw` run.
This is a temporary compatibility adapter, not a statistical module.
