# Tests

Request validation, rank mapping, command determinism, output parsing, downstream
acceptance, and the isolated stub graph live in
`tests/native_chipseq_consensus/`. Real-runtime validation is executed
separately with the pinned provider.
