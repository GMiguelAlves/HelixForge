# Tests

Input/request validation and explicit not-implemented output tests live in
`tests/native_chipseq_consensus/test_consensus_api.py`. The isolated IDR stub
must never create a consolidated peak file.
