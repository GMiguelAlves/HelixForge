# TRACK_CONTEXT

Fail-fast preflight for Track Generation API v1. It associates staged BAMs,
indexes, and metadata through final-BAM manifest IDs, validates exact reference
contigs, and never repairs IDs, builds, contigs, or upstream policies.
