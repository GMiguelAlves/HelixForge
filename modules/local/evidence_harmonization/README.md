# Evidence Harmonization

Creates deterministic entity, contrast and mark maps from two staged Evidence
Model bundles. It validates reference compatibility and never scans published
result directories. Alias and version normalization require an explicit policy.
