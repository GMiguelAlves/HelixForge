# STAR_ALIGN tests

The executable harness is under `tests/native_alignment/`. It validates the
stub contract, a tiny real alignment, semantic regression against the unchanged
legacy command, and cache invalidation behavior.
