# CHIPSEQ_REFERENCE_BUNDLE

Creates the tracked reference contract used by native ChIP-seq annotation,
tracks, and reporting. It validates inputs and records SHA-256 checksums; it
does not download, transform, or index reference data.
