# CHIPSEQ_METADATA

Validates ChIP-seq sequencing records without assuming an organism or a fixed
optional-column set. The validator resolves FASTQs, distinguishes biological
and technical identities, verifies explicit controls and emits a deterministic
plan for generic QC and Alignment APIs.

