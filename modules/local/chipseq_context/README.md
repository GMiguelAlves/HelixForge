# CHIPSEQ_CONTEXT

Compatibility adapter that sources the existing ChIP-seq configuration once
and emits a tracked snapshot. It does not execute a scientific tool or submit a
job. Native modules consume the snapshot instead of sourcing shell settings.

