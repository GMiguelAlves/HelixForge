# Candidate Scoring

Exposes all twelve legacy score components and emits a deterministic ranking.
The score is a non-inferential prioritization heuristic. Optional biological
annotations enter through one explicit, checksummed context table.
