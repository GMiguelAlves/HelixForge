# BAM_SELECT

Applies only an explicit SAMtools selection policy. It validates BAM/BAI
integrity, coordinate sort order and exact reference contig names/lengths before
filtering. It never handles duplicates or blacklist intervals.

