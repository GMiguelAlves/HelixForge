# PEAK_QC_CONTEXT

Validates the association between one final treatment BAM and one peak set using
stable record and peak identities. It also validates peak structure and
reference bounds, resolves the read/fragment unit, and records every filter.

This module never changes peak coordinates and never calculates FRiP.
