# RUN_MANIFEST tests

Contract tests live in `tests/integration_api`. They validate both assay
examples, invalid identities/context, filesystem checks and RNA/ChIP reference
compatibility. Top-level stub tests exercise process wiring.
