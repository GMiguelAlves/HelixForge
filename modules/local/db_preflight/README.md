# DB_PREFLIGHT

Fails before expensive jobs when Consensus, BAM, sample, replicate, design,
batch, filter or contrast identities are unsafe. It constructs a deterministic
comparison union from compatible semantic Consensus intervals and emits separate
count, model and contrast specifications for granular caching.
