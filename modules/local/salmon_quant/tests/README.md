# SALMON_QUANT tests

The executable harness is under `tests/native_quantification/`. It compares the
unchanged legacy Salmon command with this process, normalizing only paths and
runtime metadata, and validates deep-cache invalidation.
