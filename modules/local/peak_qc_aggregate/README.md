# PEAK_QC_AGGREGATE

Joins FRiP and peak-statistics manifests by stable peak ID and emits one row per
replicate. It does not pool BAMs, call consensus peaks, perform IDR, rank
replicates, remove outliers, or modify peak sets.
