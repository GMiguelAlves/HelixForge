# REPORT_GENERATOR

Provider `html_v1` renders the presentation-neutral aggregate as a
self-contained HTML report and structured JSON. Presentation values are inputs
only to this module, so layout changes do not invalidate report context or
scientific aggregation.
