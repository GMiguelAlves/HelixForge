# STAR_INDEX tests

The executable module harness lives in `tests/native_alignment/`. It covers
stub output shape, real index construction on a tiny genome, cache reuse, and
reference/parameter invalidation.
