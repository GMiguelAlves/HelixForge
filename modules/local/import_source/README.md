# IMPORT_SOURCE

Validates the provider identity, semantic role, filename, and checksum declared
by an Alignment or Quantification API manifest. The copied artifact and source
record have a unique per-sample directory name, which lets aggregation modules
stage many identically named provider files without collisions.
