# Native Track Generation tests

- `test_tracks.py`: parameters, exact contigs, manifest-declared artifact
  association, and order-independent aggregate joins;
- `run_stub.sh`: isolated provider/subworkflow, `-resume`, top-level native mode,
  and explicit legacy fallback.

No real BigWig, deepTools install, biological dataset, benchmark, Slurm,
container build, IGV review, or legacy regression is performed.
